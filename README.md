# chikin

Real (non-headless) Google Chrome in a Docker container, exposing the Chrome DevTools Protocol on port 9222. Suitable for automation where the browser should not self-identify as headless.

Details filled in by later tasks.
