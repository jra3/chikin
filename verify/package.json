{
  "name": "chikin-verify",
  "version": "0.1.0",
  "description": "Verify a chikin container is serving a non-headless Chrome on CDP.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/",
    "verify": "node verify.js"
  },
  "dependencies": {
    "chrome-remote-interface": "^0.33.0"
  }
}
