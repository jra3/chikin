{
  "name": "chikin-verify",
  "version": "0.1.0",
  "description": "Verify a chikin browser is serving a non-headless Chrome (fleet via gateway MCP, or a container via CDP).",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "verify": "node verify-fleet.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  }
}
